repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Keep rev in sync with ruff version in uv.lock
    rev: v0.15.5
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(src|tests|examples)/
      - id: ruff-format
        files: ^(src|tests|examples)/
